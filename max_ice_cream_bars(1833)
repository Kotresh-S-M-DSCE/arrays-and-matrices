class Solution {
    public int maxIceCream(int[] costs, int coins) {
        Arrays.sort(costs);
        int i=0,count=0;
        while(i<costs.length&&coins>0)
        {
            count++;
            coins-=costs[i++];
        }
        if(coins<0)
           count--;
        return count;
    }
}